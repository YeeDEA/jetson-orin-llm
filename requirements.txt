# Colab-side stack (notebooks installed latest-at-the-time, Dec 2025; loose pins below)
torch>=2.4
transformers>=4.44
peft>=0.12
trl>=0.9
bitsandbytes>=0.43
datasets>=2.20
huggingface_hub>=0.24

# TensorRT-LLM comes from NVIDIA's index (pre-release channel used in the notebooks):
#   pip install tensorrt_llm -U --pre --extra-index-url https://pypi.nvidia.com
# On the Jetson Orin, use the container instead: dustynv/tensorrt_llm:r36.2.0
